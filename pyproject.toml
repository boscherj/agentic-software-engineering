[project]
name = "agentic-software-engineering"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jackie Boscher", email = "jboscher@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
agentic-software-engineering = "agentic_software_engineering:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
